# Needs Tony

Current action:

- Create a HatchVision-owned GitHub repo named `gpt-codex-awo`.
- Do not initialize it with files.
- Give Codex the repo URL so it can push the local baseline.
- Import the repo into Vercel with root directory `apps/web`.

Upcoming Tony actions:

- Create HatchVision-owned Supabase project for V0.1.
- Create HatchVision-owned GitHub repo when we are ready to publish.
- Create HatchVision-owned Vercel project when we are ready to deploy.

Do not paste secrets into chat. When credentials are needed, add them directly to local `.env.local` or the relevant service dashboard.
